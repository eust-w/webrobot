{
  "name": "webrobot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "NO_PROXY=localhost,127.0.0.1 HTTP_PROXY= HTTPS_PROXY= playwright test",
    "test:e2e": "NO_PROXY=localhost,127.0.0.1 HTTP_PROXY= HTTPS_PROXY= playwright test",
    "test:install": "playwright install chromium"
  },
  "dependencies": {
    "cannon-es": "^0.20.0",
    "three": "^0.184.0",
    "urdf-loader": "^0.13.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "vite": "^8.0.16"
  }
}
